See readme.md for project overview.

If there is something missing/wrong, such as "the tests won't run" or "a workaround is needed" or "I don't have permissions to run this", use a big red X to make sure the user doesn't miss it.
